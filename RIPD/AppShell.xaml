<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RIPD.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:RIPD"
    xmlns:pages="clr-namespace:RIPD.Pages"
    Shell.FlyoutBehavior="Disabled"
    Title="RIPD">

  <TabBar>
    <Tab Title="Home">
      <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    </Tab>
    <Tab Title="Feed">
      <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    </Tab>
    <Tab Title="Add to Diary">
      <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    </Tab>
    <Tab Title="Profile">
      <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    </Tab>
    <Tab Title="Settings">
      <ShellContent
        ContentTemplate="{DataTemplate pages:SettingsPage}"
        Route="MainPage" />
    </Tab>
  </TabBar>

</Shell>
