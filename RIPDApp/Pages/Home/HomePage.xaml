<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:RIPDApp.ViewModels"
             x:Class="RIPDApp.Pages.HomePage"
             x:DataType="vm:HomeVM">
  <VerticalStackLayout>
    <Button Text="Diary" Command="{Binding GoToDiaryCommand}"/>
    <Button Text="Run" Command="{Binding GoToRunCommand}"/>
  </VerticalStackLayout>
</ContentPage>