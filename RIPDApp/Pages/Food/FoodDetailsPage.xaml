<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:RIPDApp.ViewModels"
             xmlns:m="clr-namespace:RIPDShared.Models;assembly=RIPDShared"
             xmlns:v="clr-namespace:RIPDApp.Views"
             x:Class="RIPDApp.Pages.FoodDetailsPage"
             x:DataType="vm:FoodDetailsVM">
  <Grid RowDefinitions="*,auto">

    <Grid Grid.Row="0">
      <v:FoodDetailsV x:DataType="vm:FoodDetailsVM" IsVisible="{Binding PageModeView}"/>
      <v:FoodUpdateV x:DataType="vm:FoodDetailsVM" IsVisible="{Binding PageModeUpdate}"/>
      <v:FoodCreateV x:DataType="vm:FoodDetailsVM" IsVisible="{Binding PageModeCreate}"/>
    </Grid>

    <!--<Grid Grid.Row="1">
      <Label Text="Buttons"/>
      <Button Text="Edit" Command="{Binding SwitchToUpdateModeCommand}" IsVisible="{Binding PageModeView}"/>
      <Button Text="Save" Command="{Binding SwitchToViewModeCommand}" IsVisible="{Binding PageModeEdit}"/>
      <Button Text="Create" Command="{Binding CreateFoodCommand}" IsVisible="{Binding PageModeCreate}"/>
    </Grid>-->
      
  </Grid>
</ContentPage>